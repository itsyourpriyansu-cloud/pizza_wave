import { ArrowLeft, Waves } from 'lucide-react'
import { Link, useLocation } from 'react-router-dom'
const titles: Record<string, string> = { search: 'Search', product: 'Product details', build: 'Build your pizza', cart: 'Your cart', auth: 'Wave sign in', checkout: 'Checkout', payment: 'Payment', orders: 'Orders', rewards: 'Wave Rewards', offers: 'Offers', refer: 'Share the Wave', celebrations: 'Celebrations', family: 'Family', favourites: 'Favourites', 'saved-orders': 'Saved orders', 'wave-id': 'My Wave ID', profile: 'You', support: 'Support', history: 'Points history', preferences: 'Food preferences', addresses: 'Addresses', notifications: 'Notifications' }
export default function StagePlaceholderPage() { const location = useLocation(); const parts = location.pathname.split('/').filter(Boolean); const key = parts.at(-1) ?? ''; const routeKey = ['product', 'build', 'orders'].find((candidate) => parts.includes(candidate)) ?? key; const title = titles[key] ?? titles[routeKey] ?? 'Coming next'; return <section className="placeholder-page"><Waves /><span>STAGE 2</span><h1>{title}</h1><p>This route is ready and isolated. Its complete customer flow is intentionally waiting for the next approved stage.</p><Link className="button button-secondary" to="/app/menu"><ArrowLeft size={18} /> BACK TO MENU</Link></section> }
